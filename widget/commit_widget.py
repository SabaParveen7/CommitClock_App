"""
commit_widget.py — STUB (Stretch goal, PDR 5.8)

Not implemented. See widget/android_service_notes.md for what's needed
to build this out. Left here so the import path referenced in the PDR
file structure exists and the intent is documented in code, not just
in the design doc.
"""

def update_widget(next_entry):
    """
    Intended to push `next_entry` (a commits.json row) into the home
    screen widget's RemoteViews. Currently a no-op — wire this up once
    the native AppWidgetProvider glue described in
    android_service_notes.md exists.
    """
    pass
